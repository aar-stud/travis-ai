# TRAVIS AI Services - Python 3.10 Linux (Docker)
# -----------------------------------------------------------------------
# INSTALL ORDER:
# 1. pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 torchtext==0.16.2 --index-url https://download.pytorch.org/whl/cpu
# 2. pip install -r requirements.txt
# 3. pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
# -----------------------------------------------------------------------

# PyTorch family — must be installed from torch CPU index (step 1 above)
# torchtext must come from the same index as torch — no matching PyPI wheel
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2
torchtext==0.16.2

# FastAPI
fastapi==0.109.2
uvicorn[standard]==0.27.1

# pydantic v1 — required by spacy and chromadb 0.4.x
pydantic==1.10.14

# -----------------------------------------------------------------------
# numpy + scipy pinned together — they share a C ABI boundary.
# numpy==1.24.4  required by torch 2.1.2 (numpy 2.x breaks torch).
# scipy>=1.11    requires numpy>=1.25 — crashes with 1.24.4.
# scipy==1.10.1  is the last release supporting numpy 1.24.x.
# -----------------------------------------------------------------------
numpy==1.24.4
scipy==1.10.1

# -----------------------------------------------------------------------
# scikit-learn==1.6.x pulls scipy>=1.11 as a transitive dependency.
# scikit-learn==1.3.2 is the last release compatible with scipy==1.10.1.
# The classifier .pkl files were trained with sklearn and load fine here.
# -----------------------------------------------------------------------
scikit-learn==1.3.2

# -----------------------------------------------------------------------
# transformers must be <=4.35.2.
# 4.36+ uses torch.utils._pytree.register_pytree_node which was only
# added in torch 2.3 — not present in torch 2.1.2.
# -----------------------------------------------------------------------
transformers==4.35.2
tokenizers==0.15.2
huggingface_hub==0.19.4
accelerate==0.24.1

# RAG / embeddings
sentence-transformers==2.7.0
onnxruntime==1.16.3

# chromadb 0.4.24 — stable schema; 0.5+ changed SQLite schema
chromadb==0.4.24

# spacy — English tokenizer for translation pipeline
spacy==3.7.4

# Translation fallback when custom model unavailable
deep-translator==1.11.4

# TTS
gTTS==2.5.1

# Misc
regex==2023.12.25
nltk==3.8.1